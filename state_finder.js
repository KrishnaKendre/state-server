var _ = require('lodash');

module.exports = {

    state_finder : function (points, states) {
        for (let state of states){
            if (point_checker(point, state)) {
                
            }
        }

        return "Your not part of the United States sorry!"
    }
}